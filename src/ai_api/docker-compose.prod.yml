services:
  ai_api:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    container_name: noteck_ai_api_prod
    ports:
      - "8000:8000"
    env_file:
      - .env.prod
    restart: unless-stopped
